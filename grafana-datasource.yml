apiVersion: 1

datasources:
  - name: MCP Server Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:3001
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 5s
      queryTimeout: 60s
      httpMethod: POST
    secureJsonData: {}
